// Thin re-export so callers don't import 'react-native-gesture-handler'
// directly (see GestureRootView.windows.tsx for why).
export { GestureHandlerRootView as default } from 'react-native-gesture-handler';
